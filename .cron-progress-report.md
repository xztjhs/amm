# AMM 项目巡检报告（2026-08-07 18:28）

> ⚠️ 本报告由 cron 自动巡检生成，因会话可见性限制无法直接发送到主会话，已落盘在此文件。

---

## 1. 代码提交与推送
✅ `git push origin master` 成功（c2a5c3b → Gitea iei/amm）
✅ 仓库状态干净，无未提交变更、无 lock 残留、无 merge 冲突

## 2. 近15分钟变化
- `c2a5c3b` docs: DEVELOPMENT 补充 T2V/I2V v0.3/v0.4 验证状态记录（I2V加载修复、帧处理兼容 WanPipelineOutput）
- `85cc08b` feat(v0.4): 新增模型自动下载桥接(HF/ModelScope) + GGUF 量化转换工具(llama-quantize，支持17种精度含 q4_k_m) + I2V 加载修复 + Settings 面板

## 3. 健康状态
- ⚠️ **AMM Docker 容器未运行**（`docker ps` 无 amm-server）
- ⚠️ **`/data/models` 目录为空** — Wan2.2-T2V-A14B、Wan2.2-I2V-A14B、Qwen-Image-2512 等模型尚未下载
- ⚠️ **Diffusers 引擎未安装**（engines_installed 目录不存在）
- ✅ vLLM 0.22.1 CUDA13/Blackwell 验证已通过（DEVELOPMENT 记录）
- ✅ T2V 冷启动验证已通过（峰值15.9G显存，FP8+CPU offload）
- ✅ I2V 加载修复已完成（去掉硬编码 variant=bf16）
- ✅ 日志无报错（deploy/logs/ 下日志时间停留在 8/5，容器未运行故无新日志）

## 4. 自动修复
无可安全自动修复的异常（无崩溃进程、无 lock 残留、无配置错误）。

## 5. 需介入/决策
1. **模型下载**: `/data/models` 为空，是否现在执行 `deploy/scripts/download_diffusers_models.sh` 开始拉取 Wan2.2-T2V-A14B 等模型？（模型体积大，需确认存储空间和网络环境）
2. **Diffusers 引擎安装**: 是否现在安装 Diffusers 引擎并验证 T2V/I2V 端到端推理？
3. **容器启动**: 是否重启/启动 `amm-server` 容器恢复服务？

## 下一步计划建议
- 若确认启动：先 `docker compose up -d` 启动容器 → 安装 diffusers 引擎 → 执行模型下载脚本 → 验证 T2V 冷启动
- 若暂缓：继续 v0.1 代码迭代，待部署时再统一拉模型

当前仍按 v0.1 稳定推进中，等待决策是否进入部署验证阶段。
