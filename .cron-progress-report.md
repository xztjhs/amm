# AMM 项目巡检报告

> 自动生成: 2026-08-07 18:58 CST (周五)

---

## 1) Git 进度推送 ✅

- 上次提交: `18:48:25`（约10分钟前），本次无新增代码变更
- 最新 commit: `8345400` — fix(quantize): 修复源路径未绝对化+支持 --allow-requantize；量化API端到端验证通过 (q6_k→q4_0 产物 983MB)
- 推送状态: `Everything up-to-date` → 内网 Gitea (`iei/amm`) 已同步
- Git 状态清洁，无未提交文件、无 merge 冲突、无 `.git/index.lock` 残留

## 2) 开发进展（近15分钟）

- 代码库无新变更，v0.4 核心功能处于稳定状态
- 近5次 commit 统计：6 文件变更，+239/-3 行
  - 量化器修复 (`backend/engines/quantizer.py`)
  - Diffusers 验证脚本 (`scripts/verify_diffusers.py`, `scripts/norm_frames.py`)
  - CHANGELOG / DEVELOPMENT 文档同步

## 3) 服务与引擎健康状态

- **AMM 主服务**: 当前宿主机 (`claw`) 未运行
  → 设计如此：AMM 需在带 GPU + Python 3.11 的 Docker 容器内运行，当前环境无 `docker` / `python3.11` / `nvidia-smi`
- **vLLM 引擎**: 代码层就绪（`vllm-0.22.1`，历史日志确认可正常启动）
- **Diffusers 引擎**: 代码层完备（Wan2.2 MoE FP8+BF16 配置，边界比率 0.875）

## 4) 模型下载状态

- `/data/models/` 为空目录；`/models/` 目录无 Wan 相关文件
- **Wan2.2-T2V-A14B** / **I2V-A14B** 尚未下载（各约 28GB）
- 模型配置 `models_config.yaml` 已指定来源 `modelscope: Wan-AI/Wan2.2-T2V-A14B-Diffusers`
- 冷启动需等待模型下载完成后在目标 GPU 节点验证

## 5) 自动修复动作

- 无需自动修复（代码干净、无残留锁、无冲突）

## ⚠️ 需要老板介入

1. **运行环境确认**: AMM 服务应在哪个 GPU 节点 / Docker 宿主机上启动？当前 `claw` 节点无 GPU 与 Docker，无法承载推理服务。
2. **模型下载执行**: Wan2.2-T2V-A14B / I2V-A14B 需要在目标节点（具备 HuggingFace/ModelScope 下载条件与 60GB+ 磁盘空间）手动或自动拉取。

## 下一步计划

- 待运行环境确认后，在目标节点执行：`bash /amm/deploy/install.sh` → 启动容器 → 拉取模型 → 验证 T2V/I2V 推理链路
- 持续按 v0.1 稳定推进代码迭代
