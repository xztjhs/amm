# AMM 项目巡检报告

> 自动生成: 2026-08-07 19:13 CST (周五)

---

## 1) Git 进度推送 ✅

- 上次提交: `19:02:47`（约10分钟前），本次无新增代码变更
- 最新 commit: `041f12b` — auto: wip progress 2026-08-07 19:02:47
- 推送状态: `Everything up-to-date` → 内网 Gitea (`iei/amm`) 已同步
- Git 状态清洁，无未提交文件、无 merge 冲突、无 `.git/index.lock` 残留

## 2) 开发进展（近15分钟）

- 代码库无新变更，v0.4 核心功能处于稳定状态
- 上次 commit 以来的产出仍在消化中，近期重点推进项见 CHANGELOG v0.4

## 3) 服务与引擎健康状态

- **AMM 主服务 / vLLM / Diffusers**: 当前 `claw` 节点无运行中进程
  → 设计如此：AMM 推理需在带 GPU + Docker 的目标节点运行，当前环境无 `nvidia-smi` / `docker`
- **日志**: `deploy/logs/` 下均为 8月5日 日志，无新增报错；历史日志显示各引擎（llama.cpp OCR/ASR/Chat/TTS）退出正常
- **无异常进程残留**，无崩溃需重启

## 4) 模型下载状态

- **Wan2.2-T2V-A14B**: 尚未下载（`/models/zoo/modelscope/models/` 无 Wan 相关目录）
- 配置已就绪: `backend/config/models_config.yaml` 已指定 ModelScope 源 `Wan-AI/Wan2.2-T2V-A14B-Diffusers`
- **冷启动验证**: 需待模型下载完成后在目标 GPU 节点通过 `t2v_coldstart.py` 执行

## 5) 自动修复动作

- 无需修复（代码干净、无锁残留、无冲突、无崩溃进程）

## ⚠️ 待老板确认（非紧急，持续跟踪）

1. **运行环境启动**: AMM 应在哪个 GPU 节点 / Docker 宿主机上启动？当前 `claw` 纯 CPU 无法承载推理。
2. **模型下载执行**: Wan2.2-T2V-A14B（约28G）需在目标节点通过 `deploy/scripts/dl_one.py` 或 WebUI「模型自动下载」面板拉取。

## 下一步计划

- 持续按 v0.4 推进（Diffusers 验证已闭环，量化 API 已通，下载桥接已就绪）
- 待运行环境和模型就绪后，执行冷启动验证 → v0.5 迭代
