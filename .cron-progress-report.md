# AMM 项目巡检报告

> 自动生成: 2026-08-07 18:43 CST (周五)

---

## 1) Git 进度推送 ✅

- 上次提交: `18:36:13`（约7分钟前），本次无新增变更
- 推送状态: `Everything up-to-date` → 内网 Gitea (`iei/amm`) 已同步
- Git 状态清洁，无未提交文件、无 merge 冲突、无 `.git/index.lock` 残留

## 2) 开发进展（近15分钟）

- 最后有效 commit: `b9c24e3` —— 仅更新了本巡检报告文件（36行新增）
- v0.4 核心代码已于此前完成：
  - 模型自动下载(HF/ModelScope)
  - GGUF量化转换工具(17种精度)
  - I2V加载修复
  - download_bridge/quantizer API + Settings面板
- 文档同步: DEVELOPMENT 已记录 T2V/I2V v0.3/v0.4 验证状态

## 3) 服务与引擎健康状态

- **AMM 主服务**: 当前宿主机(`claw`)未运行
  → 属正常：项目设计为 Docker 容器运行，当前宿主机无 `docker` 命令
- **vLLM 引擎**: ✅ 安装成功（`vllm-0.22.1`，日志确认）
- **Diffusers 引擎**: 代码层已完备（Wan2.2 MoE FP8+BF16 配置）

## 4) 模型下载状态

- `/data/models/` 为空目录
- Wan2.2-T2V-A14B / I2V-A14B 尚未下载（约 28GB × 2）
- 需确认：模型下载应在哪个 GPU 节点执行？

## 5) 自动修复动作

- 无需自动修复

## 下一步

1. 确认 AMM 实际运行节点并检查服务状态
2. 在使用节点启动 Wan2.2 模型下载（ModelScope 源）
3. 模型就位后验证 T2V/I2V 推理通路
