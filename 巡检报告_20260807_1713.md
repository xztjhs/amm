# AMM 项目 15分钟例行巡检报告 (2026-08-07 17:13)

## 1) Git 状态
- 工作区干净，无未提交/未推送变更
- 最新提交: `cc13cea` — ui(nav): 顶部菜单删除 GPU 项 (GPU 状态保留在 Dashboard 第二行)
- Push: `Everything up-to-date` → 内网 Gitea(iei/amm) 已同步

## 2) 近15分钟变化
- 17:01 上轮巡检已确认 v0.1 稳定
- 最新实质提交 16:47 为前端调整 (index.html)，无后端变更
- 整体仍在 v0.1 稳定推进中

## 3) 健康检查
- ✅ Git 无冲突、无 index.lock、无坏状态
- ✅ 宿主机无异常进程或僵尸进程
- ℹ️ AMM 主进程/引擎未在宿主机运行 — 符合容器化架构预期（server.py 依赖 Python 3.11 + CUDA，宿主机为 Python 3.9 且无 Docker）
- ℹ️ 日志目录 `deploy/logs/` 最后更新 8月5日，对应容器内运行时期
- ✅ Wan2.2-T2V 验证视频存在 (`verification/wan_t2v_480p_16steps_17frames_20260807-1121.mp4`)，冷启动此前已跑通
- ⚠️ `/models` 与 `/amm` 挂载点在宿主机不可见，模型下载进度需在目标容器/节点内确认

## 4) 自动修复
- 本次无可安全自动修复的异常

## 5) 下一步计划
- 等待 GPU/Docker 运行时环境恢复后，在容器内做端到端验证
- 如需确认 Wan2.2-T2V-A14B 模型完整下载状态，需登入运行容器执行 `ls -R /models/Wan-AI/`
- 持续按 v0.1 稳定推进
