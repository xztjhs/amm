# AMM 项目 15分钟巡检报告

**巡检时间**: 2026-08-07 16:21  
**巡检人**: cron (auto)

---

## 1) 提交推送 ✅

- 已提交并推送至内网 Gitea (`iei/amm`): `35f88be auto: wip progress 2026-08-07 16:23:09`
- origin/master 同步，无冲突，无 index.lock 残留

## 2) 健康状态检查

| 检查项 | 状态 |
|--------|------|
| Git 仓库状态 | ✅ 干净，无 merge 冲突 |
| vllm/diffusers 进程 | ⚠️ **无进程运行** |
| AMM 服务进程 | ⚠️ **无容器/无服务** |
| deploy/logs/ | 日志截止 8月5日 16:33，无新增报错 |
| /data/models | ⚠️ **空目录**，无模型文件 |
| 磁盘空间 | ✅ 充足 (498G，已用 17G/4%) |

## 3) 近15分钟变化

- 代码层面无新提交（上个代码提交 `7d17951` 是 v0.2 模型文件选择升级）
- 仅新增一份巡检报告归档

## 4) 关键异常与需要老板介入的点 🚨

1. **运行环境问题**：当前主机 (`claw`) **无 Docker、无 NVIDIA GPU**，AMM 服务（依赖容器+GPU）无法在此启动。AMM 实际应在哪台机器运行？
2. **模型文件缺失**：`/data/models` 为空。Wan2.2-T2V-A14B 冷启动验证曾成功（`verification/wan_t2v_480p_16steps_17frames_20260807-1121.mp4` 存在），但模型文件目前不在此机器。如需复现/继续开发，需确认模型存储位置或重新下载（~28-118G）。
3. **服务已停 2 天**：最后日志为 8月5日 16:33，容器已不存在。如需恢复服务，需在正确的 GPU 机器上执行 `docker compose up -d`。

## 5) 自动修复

- 仅执行了 Git 提交推送，无其他可安全自动修复项（启动容器、下载大模型需确认环境，未擅自操作）。

## 6) 下一步计划

- 等待老板确认 AMM 目标运行主机
- 确认后可在该主机恢复 docker-compose 并检查模型下载状态
