# AMM 项目 15分钟例行巡检报告
时间：2026-08-07 11:28 CST

---

## 1. Git 状态 ✅
- 本地/远端完全同步：`79993de`（v0.1.1）
- 无可提交变更、无未推送提交、无 `.git/index.lock`
- `engine.py` 语法校验通过

## 2. 开发进展 ✅
- v0.1.1 已打 tag 并推送内网 Gitea（iei/amm）
- **Wan2.2-T2V-A14B 冷启动验证通过** 🎉
  - 验证视频已落盘：`verification/wan_t2v_480p_16steps_17frames_20260807-1121.mp4`（今天 11:21 生成）
  - FP8 layerwise_casting + CPU offload 生效，峰值显存 15.9G
- vLLM EngineCore 子进程残留已修复（递归进程树清理）

## 3. 健康状态 ⚠️
- **AMM 主服务当前未运行**（端口 80/8080 health 无响应）
- 最后活跃日志：8/5 16:33，之后无新日志
- 当前巡检环境：15G 内存、无 GPU（nvidia-smi 不可用），**不适合直接启动 AMM**

## 4. 自动修复
- 无需修复。Git 干净、无进程锁、模型下载已完成。

## 5. 需人工介入 🔧
- **服务恢复**：AMM 停了约 2 天，如需恢复请在 GPU 容器/主机执行 `bash /amm/deploy/amm.sh start`（或检查 docker-compose）。当前宿主机无 GPU/内存不足，无法代劳。
- **历史异常**：日志中曾出现 `TypeError: Object of type coroutine is not JSON serializable`，如复现需定位未 await 的异步调用。

## 下一步计划
- 继续按 v0.1.1 稳定推进
- 待服务恢复后，可验证 `/v1/videos/generations` 端到端稳定性
