# AMM 项目巡检报告

**巡检时间：** 2026-08-08 17:37 (Asia/Shanghai)  
**项目路径：** `/data/xtao/amm`  
**巡检周期：** 15分钟例行巡检  
**版本状态：** v0.6 稳定推进中

---

## 1. Git 推送情况

| 项目 | 状态 |
|---|---|
| 工作区状态 | ✅ 干净（无未提交/未跟踪文件） |
| 最近一次提交 | `6a4d2fd` auto: wip progress 2026-08-08 17:24:32 |
| 远程仓库 | ✅ origin → `http://192.168.100.245:60005/iei/amm.git` |
| 推送状态 | ✅ 已同步至内网 Gitea (iei/amm) |
| 冲突/坏状态 | ✅ 无 merge 冲突、无 `.git/index.lock` |

> 本次巡检区间内（17:24 → 17:37）无新增代码变更，上一次 auto wip progress 已于 17:24 成功提交并推送。

---

## 2. 近15分钟变更

- **代码变更：** 无（工作区干净）
- **提交记录：** 上一轮 17:24 的 `auto: wip progress` 为最新提交
- **配置文件：** `backend/config/models_config.yaml` 最后更新于 08-08 12:03（2.13MB），无变动

---

## 3. 健康状态

### 3.1 服务进程

| 检查项 | 状态 | 说明 |
|---|---|---|
| AMM 主服务 | ⚠️ 未运行 | 端口 8080 无监听 |
| vLLM 引擎 | ⚠️ 未运行 | 端口 18081 无监听，无相关 Python 进程 |
| diffusers 引擎 | ⚠️ 未运行 | 无相关进程 |
| GPU 环境 | ⚠️ 不可用 | `nvidia-smi` 未找到 |

> 日志最后更新于 **08-05 16:33**，此后无新日志写入。当前 AMM 各服务组件均未处于运行状态。

### 3.2 模型文件

- `/models` 目录：**不存在**
- 项目内 `models/` 目录：**不存在**
- 磁盘空间：**充足**（477G / 498G 可用，使用率 5%）

### 3.3 系统环境

- 操作系统：Linux 5.14.0-687.17.1.el9_8.x86_64
- Python 系统进程：`tuned` / `tuned-ppd` 正常
- SSH 服务：正常（端口 22 监听中）

---

## 4. 自动修复动作

| 检查项 | 结果 |
|---|---|
| `.git/index.lock` 清理 | ✅ 无需清理（不存在） |
| merge 冲突修复 | ✅ 无需修复（无冲突） |
| 崩溃引擎重启 | ⏸️ **跳过** — 服务当前未运行，且涉及模型路径/配置/GPU 环境，需人工确认启动策略 |
| 模型下载续传 | ⏸️ **跳过** — 无 `/models` 目录及下载任务记录，需人工确认目标模型与存储路径 |
| 配置错误修复 | ✅ 未检测到明显配置错误 |

---

## 5. 需老板介入点

1. **服务启动策略：** AMM 主服务及 vLLM 引擎当前均未运行。需确认：
   - 是否为预期状态（开发暂停 / 环境维护）？
   - 若需恢复服务，请确认 `/models` 路径映射及目标模型文件是否就绪。

2. **GPU 环境：** 当前节点无 `nvidia-smi`，若后续需启用 vLLM 推理，请确认 GPU 驱动与 CUDA 环境。

3. **模型下载：** `/models` 目录缺失。如需下载 Qwen3-4B 或其他模型，请提供 HuggingFace / 镜像站凭据及目标路径。

---

## 6. 下一步计划

- [ ] 待老板确认后，恢复 AMM 服务及引擎启动
- [ ] 配置 `/models` 目录挂载或模型下载任务
- [ ] 验证 vLLM / diffusers 引擎健康状态
- [ ] 恢复定时健康检查接口（`GET /api/health`）

---

*本次巡检由 cron 自动执行，报告随 git 自动提交。*
