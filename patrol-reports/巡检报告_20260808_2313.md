# AMM 项目巡检报告

- **巡检时间**: 2026-08-08 23:13 (Asia/Shanghai)
- **巡检周期**: 15分钟例行巡检
- **项目路径**: /data/xtao/amm
- **当前版本**: v0.7.1-24-gbbd7e75

---

## 1. Git 推送情况

| 检查项 | 结果 |
|--------|------|
| 未提交变更 | ✅ 无 |
| 未推送提交 | ✅ 无 |
| 最新提交 | `bbd7e75 auto: wip progress 2026-08-08 23:01:41` |
| Push 状态 | ✅ Everything up-to-date（已同步至内网 Gitea `iei/amm`） |
| .git/index.lock | ✅ 无残留 |
| 冲突/坏状态 | ✅ 无 |

**近15分钟变更**: 无代码变更，工作区干净。上一次提交为 `bbd7e75`（23:01:41），仅新增上一轮巡检报告。

---

## 2. 服务健康状态

| 检查项 | 结果 |
|--------|------|
| AMM 主进程 | 🔴 **未运行** |
| Chat 推理服务 (vllm) | 🔴 **未运行** |
| ASR / OCR / Embedding / TTS (llama_server) | 🔴 **均未运行** |

**日志分析**: `deploy/logs/*.log` 最后更新仍为 **2026-08-05 17:07** 左右，距今约 **3 天**。无近15分钟新日志。无 ERROR / WARNING / Exception 痕迹。

**结论**: AMM 全套服务持续停运（与上轮巡检状态一致），非近期崩溃，推测为 8/5 被主动停止后未恢复。

---

## 3. 模型下载 / 磁盘状态

| 检查项 | 结果 |
|--------|------|
| 进行中下载 (`.tmp`/`.part`/`.download`) | ✅ 无 |
| 磁盘空间 | 未在本机检测到异常 |

---

## 4. 自动修复动作

本轮巡检 **未执行自动修复**。原因：

1. Git 状态健康，无需处理。
2. 服务停运原因不明（主动维护 / 宿主机迁移 / 配置调试？），重启涉及宿主机 Docker 环境判断，非本机可直接操作（`docker` 命令不可用）。贸然远程启动风险不可控。
3. 无模型下载中断、无配置损坏、无 `.git` 残留。

---

## 5. 需老板介入点

- [ ] **🔴 核心**: AMM 全套服务已停运约 3 天（自 8/5），请确认是否为计划内维护。若非计划内，请安排重启并排查停机根因。
- [ ] 当前代码版本已推进至 **v0.7.1-24**（上轮报告为 v0.6），推测近期有开发更新 pushed 到 master，但尚未在运行时验证。
- [ ] 如需恢复服务，可参考 `deploy/amm.sh start`；建议恢复后做一次 v0.7.1 功能回归验证。

---

## 6. 下一步计划

1. 待老板确认后，在宿主机（192.168.100.245）执行服务重启与端口健康检查。
2. 服务恢复后，验证 v0.7.1 chat / ocr / asr / embedding / tts 各模块。
3. 恢复正常巡检节奏，监控进程与日志实时输出。
