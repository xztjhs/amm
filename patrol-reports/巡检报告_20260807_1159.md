# AMM 项目 15分钟例行巡检报告
时间：2026-08-07 11:59 CST

---

## 1. Git 进度提交 ✅
- 已在 11:55:58 自动提交并推送至内网 Gitea `iei/amm`
- HEAD：`d0b9690`（v0.1.1）
- 本地/远端同步，无未推送提交，无 `.git/index.lock`

## 2. 近15分钟变化
- 相比 11:44 巡检，本轮主要变化：`deploy/docker-entrypoint.sh` SSH host key 逻辑优化（仅在缺失时生成，避免容器重启后 known_hosts 指纹变化）
- 无其他实质代码变更

## 3. 健康检查
- **进程状态**：❌ 当前无 vllm/diffusers 进程，AMM 主服务未启动
- **Docker 状态**：❌ 无 amm 容器运行
- **日志状态**：最后活跃日志 8/5 16:33，之后无新日志
- **模型缓存**：当前宿主机无 `/models/zoo/modelscope` 目录（非 GPU 主机）
- **Wan2.2-T2V**：验证视频 `verification/wan_t2v_480p_16frames_20260807-1121.mp4` 已落盘（132K，尺寸异常偏小，可能为测试片段或失败产物）
- **异常检查**：✅ 无 merge 冲突、无残留锁文件、无崩溃进程

## 4. 自动修复
- 无异常可安全自动修复。服务未运行系当前宿主机无 GPU / 非目标部署环境，无法代劳拉起。

## 5. 需介入事项 🔧
- **AMM 服务恢复**：已停机约 2 天，如需恢复请在 GPU 主机执行启动（`docker compose up` 或 `python backend/server.py`）。当前环境不具备 GPU，cron 未自动拉起。
- **Wan2.2-T2V 验证视频**：11:21 产出的视频仅 132KB，疑似未真正生成有效内容，需在 GPU 环境重新冷启动验证。

## 6. 下一步
- 仍按 v0.1.1 稳定推进。近15分钟无实质功能进展，当前聚焦容器化部署脚本打磨（entrypoint + compose）。
