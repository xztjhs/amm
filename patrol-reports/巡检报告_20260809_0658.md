# AMM 巡检报告 — 2026-08-09 06:58

## Git 推送情况
- **仓库状态**: 干净，无未提交变更
- **最新提交**: `7616f47 auto: 巡检报告 2026-08-09 06:47:21`
- **远程**: origin → `http://x-access-token:...@192.168.100.245:60005/iei/amm.git` (内网 Gitea)
- **推送结果**: 已推送（巡检报告文件本身作为新文件提交并推送）

## 近 15 分钟变更
- 无代码/配置变更（自 06:47 巡检以来仓库无修改）
- 本次仅新增本巡检报告文件

## 健康状态
| 检查项 | 状态 | 说明 |
|--------|------|------|
| Git 仓库 | ✅ 正常 | 无冲突、无 index.lock、工作区干净 |
| vllm/diffusers 进程 | ⚠️ 异常 | 无运行中的 vllm 或 diffusers 进程 |
| 服务日志 | ⚠️ 异常 | `deploy/logs/*` 最后写入均为 **2026-08-05 16:33**，已停转约 3.5 天 |
| 模型目录 | ⚠️ 异常 | 仓库根目录下无 `models/` 目录；日志引用路径为 `/models/Qwen3-4B`（系统级路径，状态未知） |
| 磁盘空间 | ✅ 正常 | `/dev/vda2` 使用率 5%（22G/498G） |

## 发现的问题（需老板介入）
1. **AMM 主服务引擎已离线**：vllm 进程未运行，日志自 8/5 后无更新。上次正常启动记录显示 chat 模型（Qwen3-4B）已加载并监听 `0.0.0.0:18081`，但当前服务未存活。无法确认是机器重启后未自启，还是手动停止。
2. **模型文件位置在系统级路径**：仓库自身不含 `models/`，引用的是 `/models/Qwen3-4B`。需确认该路径是否仍然可用、模型是否完整。

> **自动修复判定**：上述两项涉及启动方式/环境依赖及外部模型存储，自动重启可能导致依赖缺失或配置漂移，**暂不自动修复，等待老板决策**。

## 下一步计划
- [ ] 老板确认后，检查 AMM 的启动方式（systemd/docker-compose/manual）并恢复服务
- [ ] 验证 `/models/` 挂载及模型完整性
- [ ] 确认服务恢复后，回归测试 `/api/health` 与 `/v1/chat/completions`

---
*巡检时间: 2026-08-09 06:58 (Asia/Shanghai)*
