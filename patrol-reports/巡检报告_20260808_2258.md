# AMM 项目巡检报告

- **巡检时间**: 2026-08-08 22:58 (Asia/Shanghai)
- **巡检周期**: 15分钟例行巡检
- **项目路径**: /data/xtao/amm
- **版本**: v0.6 稳定版（当前提交 `1ef7e8a`）

---

## 1. Git 推送情况

| 检查项 | 结果 |
|--------|------|
| 未提交变更 | ❌ 无 |
| 未推送提交 | ❌ 无 |
| 最新提交 | `1ef7e8a auto: wip progress 2026-08-08 22:46:11` |
| Push 状态 | ✅ Everything up-to-date（已同步至内网 Gitea `iei/amm`） |
| .git/index.lock | ✅ 无残留 |
| 冲突/坏状态 | ✅ 无 |

**近15分钟变更**: 无代码变更，工作区干净。上一次有效的代码提交是在 `6cafbc7` (fix v0.7.3 模型页 bug 等)，之后均为 auto: wip progress 空提交。

---

## 2. 服务健康状态

| 检查项 | 结果 |
|--------|------|
| AMM 主进程 (`python3.11 backend/server.py`) | 🔴 **未运行** |
| Chat 推理服务 (vllm) | 🔴 **未运行** |
| ASR 服务 (llama_server) | 🔴 **未运行** |
| OCR 服务 (llama_server) | 🔴 **未运行** |
| Embedding 服务 (llama_server) | 🔴 **未运行** |
| TTS 服务 (llama_server) | 🔴 **未运行** |
| Docker 容器 | 🔴 **无相关容器运行** |

**日志分析**: 所有日志文件 (`deploy/logs/*.log`) 最后修改时间为 **2026-08-05 17:07**，距今约 **3 天**。日志末尾显示各服务为正常退出（`cleaning up before exit...`），非崩溃。无近15分钟新日志输出。

**结论**: AMM 全套服务自 8月5日 17:07 后处于停运状态，非近期崩溃，而是之前被主动或异常停止后未重启。

---

## 3. 模型下载状态

| 检查项 | 结果 |
|--------|------|
| 进行中下载 (`.tmp`/`.part`/`.download`) | ✅ 无 |
| 磁盘空间 (简要) | 待人工确认 `/models` 及宿主导出目录 |

---

## 4. 自动修复动作

本轮巡检 **未执行自动修复**。原因如下：

1. **无 .git 残留/冲突** — 无需清理。
2. **服务全面停运** — 重启整套服务涉及容器/宿主机环境判断，且停运原因不明（主动维护？配置变更？资源抢占？）。贸然启动可能覆盖现场或导致配置冲突。
3. **无模型下载中断** — 无需续传。
4. **无异常配置错误** — 未检测到可安全自动修复的明显配置损坏。

---

## 5. 需老板介入点

- [ ] **🔴 核心**: AMM 全套服务已停运约 3 天（自 8/5 17:07），请确认是否计划内维护。若不是，请安排重启并排查停机根因。
- [ ] 如需重启，可使用 `bash /amm/deploy/amm.sh start`（容器内）或检查宿主机 Docker 启动方式。
- [ ] 建议检查 `/models` 目录挂载及磁盘空间，确保模型文件完整。

---

## 6. 下一步计划

1. 待老板确认后，执行服务重启并验证各子服务健康端口。
2. 恢复 v0.6 稳定功能验证（chat/ocr/asr/embedding/tts）。
3. 如服务恢复，后续巡检将恢复正常进程与日志监控。
