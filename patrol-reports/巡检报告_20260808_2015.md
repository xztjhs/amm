# AMM 项目巡检报告

- **巡检时间**: 2026-08-08 20:13 (Asia/Shanghai)
- **仓库路径**: /data/xtao/amm
- **当前分支**: master
- **最新提交**: 5daa1c7 fix(v0.7.3c): 推理后显存真正释放 — del pipe 局部引用

---

## 1. Git 推送情况

- 工作区状态：clean（无未跟踪/修改文件）
- HEAD 与 origin/master 完全同步，无需推送
- 近15分钟无代码变更
- 无 .git/index.lock 等残留锁文件
- 无合并冲突或坏状态

## 2. 近15分钟变更

- 无新增代码提交
- 无文件修改/删除

## 3. 健康状态

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Git 仓库 | ✅ 正常 | clean，无冲突，已同步 origin/master |
| 代码提交 | ✅ 正常 | 最新 v0.7.3c，推理后显存释放修复已合入 |
| 服务进程 | ⚠️ 异常 | 各服务端日志最后活跃于 8月5日；本地无 docker 命令，无法确认容器状态 |
| vllm/diffusers 引擎 | ⚠️ 异常 | 无运行中进程；日志停留在 8/5 16:33 |
| 模型目录 | ℹ️ 未检出 | deploy/models/ 不存在（可能宿主机通过卷挂载到 /models） |

## 4. 自动修复动作

- 无需修复 Git 状态（clean）
- **未能自动修复**：AMM 容器/服务未运行，但当前环境无 `docker` 命令，无法安全自动重启

## 5. 需老板介入点

1. **AMM 服务停机**：自 8月5日 后无新日志，疑似容器已停。请确认宿主机（192.168.100.245）上 AMM 容器状态：
   ```bash
   ssh root@192.168.100.245 -p 62022
   docker ps -a --filter name=AMM
   docker logs --tail 50 AMM
   ```
   若已退出，执行 `docker restart AMM` 恢复。

2. **运行环境确认**：当前巡检环境无 docker CLI，是否为预期（仅在宿主机管理容器）？

## 6. 下一步计划

- [ ] 确认并恢复 AMM 容器运行
- [ ] 重启后验证 `/api/health` 及各引擎状态
- [ ] 继续按 v0.6 稳定路线图推进（当前代码基线 v0.7.3c 已就绪）

---

*报告由 cron 自动生成*
