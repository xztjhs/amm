# AMM 项目巡检报告

- **巡检时间**: 2026-08-08 13:28 (Asia/Shanghai)
- **巡检周期**: 15 分钟（距上轮 13:17 约 11 分钟）
- **项目路径**: /data/xtao/amm
- **目标分支**: master → origin/iei/amm (内网 Gitea)

---

## 1. Git 推送情况

- **本地分支**: master @ `946bb83`
- **提交信息**: docs(readme): README 界面预览补充 Models 配置 + Playground 测试截图
- **工作区状态**: 干净（无未提交、无未推送、无冲突）
- **index.lock**: 无残留
- **推送结果**: 本轮无代码增量变更，无需提交 WIP

## 2. 近 15 分钟变更

- 无新增代码变更（git status --short 空白）
- 上轮自动提交 `auto: wip progress 2026-08-08 13:17:41` 已推送

## 3. 健康状态

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Git 仓库 | ✅ 正常 | 干净、无冲突、无锁残留 |
| 服务进程 | ❌ 异常 | 自 08-05 17:07 起全部离线；当前无 vllm/llama.cpp/diffusers 进程 |
| Docker 容器 | ❌ 异常 | `docker` 命令在当前宿主不可用，容器状态无法获取 |
| 模型目录 | ⚠️ 空目录 | `/data/models` 存在但为空，无模型文件 |
| 下载进程 | ✅ 正常 | 无进行中下载任务，无残留 |

> **风险点**：
> 1. AMM 服务（Web + 推理引擎）自 8 月 5 日中断后未恢复，已离线约 **3 天**。
> 2. `/data/models` 目录为空，即使重启容器也无法加载任何模型。
> 3. 当前宿主环境缺少 `docker` CLI，无法直接执行 `docker compose up`。

## 4. 自动修复动作

- **已执行**：无（(env) 条件不满足自动修复阈值）
- **未执行原因**：
  - 重启服务需 Docker 环境 + GPU 驱动 + 非空模型目录，当前任一条件均不满足。
  - 模型下载涉及外部网络/存储/授权，需人工确认下载清单与磁盘配额。
  - 无 `.git/index.lock` 等可安全清理的锁残留。

## 5. 需老板介入点

1. **服务恢复**：请确认当前宿主机是否为容器化部署节点，或联系运维恢复 `amm-server` 容器运行环境。
2. **模型补全**：请确认 `/data/models` 下模型文件是否被误删/迁移，或提供需要重新下载的模型清单。
3. **部署环境**：如当前主机并非目标部署机，请确认后续巡检是否仍需关注服务进程状态。

## 6. 下一步计划

- 若环境恢复且收到模型清单，可立即触发自动化下载与启动脚本。
- 若确认当前仅做代码开发（不承载服务），后续巡检将聚焦代码提交与测试状态，弱化进程/模型检查。
- v0.6 文档与截图工作已完成（README + Dashboard + Playground 截图），代码层面稳定，等待环境恢复即可上线验证。

---

*报告由 cron 自动生成 — AMM 项目 15 分钟例行巡检*
