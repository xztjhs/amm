# AMM 项目巡检报告

- **巡检时间**: 2026-08-09 07:58 CST
- **巡检周期**: 15分钟例行
- **项目路径**: /data/xtao/amm
- **版本状态**: v0.6 稳定版

---

## 1. Git 推送情况

| 检查项 | 状态 |
|--------|------|
| 远程仓库 | origin/master (iei/amm @ Gitea) |
| 最近提交 | `1de8028 auto: wip progress 2026-08-09 07:46:42` |
| 本地分支 | master 与 origin/master 同步 |
| 未提交变更 | 无 |
| 未推送提交 | 无 |
| index.lock | 无残留 |
| 合并冲突 | 无 |
| 推送结果 | ✅ Everything up-to-date |

---

## 2. 近15分钟变更

- **Git 提交**: `1de8028` (07:46:42) — 上轮 wip progress，距本轮约 12 分钟
- **文件变更**: 无新增未提交变更
- **巡检报告**: 上轮报告 `巡检报告_20260809_0743.md` 已提交并推送

---

## 3. 健康状态

| 服务/组件 | 期望状态 | 实际状态 | 结果 |
|-----------|----------|----------|------|
| amm-server (主服务) | 运行中 | ❌ 未运行 | **异常** |
| chat (vllm) | 运行中 | ❌ 未运行 | **异常** |
| asr (llama.cpp) | 运行中 | ❌ 未运行 | **异常** |
| embedding | 运行中 | ❌ 未运行 | **异常** |
| ocr | 运行中 | ❌ 未运行 | **异常** |
| tts | 运行中 | ❌ 未运行 | **异常** |
| diffusers (t2i/t2v/i2v) | 按需加载 | ❌ 未运行 | **异常** |

**核心异常**: AMM 全部服务实例自 **2026-08-05 17:07** 起已离线，已持续约 **4 天**。

---

## 4. 异常详情与根因分析

### 4.1 服务离线
- 日志最后更新时间: 2026-08-05 17:07
- 所有日志末尾均出现 `cleaning up before exit...`，表明服务被优雅终止
- 进程检查: 无 vllm、diffusers、python server 相关进程残留

### 4.2 运行环境缺失（无法在本机自动重启）
- **Docker**: 当前宿主机未安装 Docker，无法执行 `docker compose up`
- **Python 版本**: 宿主机为 Python 3.9.25，项目要求 Python 3.11
- **目录结构**: server.py 依赖 `/amm` 挂载路径（容器内路径），直接启动会因 `PermissionError: /amm` 失败
- **模型目录**: `/data/models/` 存在但为空，即使启动成功也无可用模型

### 4.3 直接启动测试
```
cd /data/xtao/amm && python3 backend/server.py
→ PermissionError: [Errno 13] Permission denied: '/amm'
```
原因: `AMM_ROOT` 默认为 `/amm`，而本机无此目录权限。

---

## 5. 自动修复动作

| 异常 | 可自动修复? | 执行结果 |
|------|-------------|----------|
| 清理 .git/index.lock | ✅ | 无残留，无需处理 |
| 重启崩溃引擎 | ❌ | 本机无 Docker，Python 版本不匹配，无法重启 |
| 续传模型下载 | ❌ | /data/models 为空，但无下载任务记录可续传 |
| 修复配置错误 | ❌ | 无明确配置错误迹象 |

---

## 6. 需老板介入点

1. **服务恢复**: AMM 全部服务已离线 4 天，需确认是否应恢复运行。若需恢复：
   - 需要具备 Docker + NVIDIA Container Toolkit 的环境
   - 或提供可运行 `python3.11` 且已挂载 `/data/models` 的目标宿主机
   
2. **模型文件**: `/data/models/` 当前为空，需确认模型是否应存放于此目录，或是否需要从其他位置同步/下载。

3. **部署环境**: 当前巡检所在宿主机 (`claw`) 似乎不是 AMM 的 Docker 宿主机。请确认 AMM 应在哪台机器上运行，或是否需重新部署。

---

## 7. 下一步计划

| 优先级 | 事项 | 建议 |
|--------|------|------|
| P0 | 服务恢复 | 确认目标宿主机后，执行 `docker compose up -d` 或等效启动 |
| P1 | 模型补齐 | 确认模型来源，同步/下载至 `/data/models/` |
| P2 | 环境统一 | 如需长期在本机运行，可配置 Python 3.11 虚拟环境并调整路径 |
| P3 | 监控告警 | 服务恢复后可配置自动巡检中的进程存活检测 |

---

*报告由 amm-project-progress cron 自动生成*
