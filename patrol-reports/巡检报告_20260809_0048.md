# AMM 项目 15 分钟例行巡检报告

**巡检时间**：2026-08-09 00:48 CST  
**巡检对象**：/data/xtao/amm（iei/AMM，master 分支）  
**运行环境**：AMM Docker 容器（abe5d4e0b813）

---

## 1. Git 推送情况
- **本地分支**：master @ f37a34f（feat(v0.8.0): 用户手册上线 + 版本号统一 + 容器瘦身 + amm-docker）
- **远端同步**：与 origin/master 完全一致（fetch 后无差异）
- **提交状态**：工作区干净，无未提交/未推送变更
- **推送结果**：`Everything up-to-date`（无需推送）

## 2. 近 15 分钟变更
- 本地仓库在最近 15 分钟内无任何文件变更、无新提交。
- 远端 master 亦未收到新推送。

## 3. 健康状态
| 检查项 | 结果 | 说明 |
|--------|------|------|
| `.git/index.lock` | ✅ 正常 | 无残留锁文件 |
| Git 冲突/坏状态 | ✅ 正常 | `git status --short` 为空，无 merge/rebase 冲突 |
| AMM 主进程 | ⚠️ 需关注 | `python3.11 /amm/backend/server.py`（PID 56）运行中，已启动约 18 分钟，`curl localhost:80` 返回 **200**，HTTP 服务响应正常。 |
| 日志目录 | ⚠️ 异常 | 容器内 `/amm/deploy/logs/` 目录在本次巡检过程中**凭空消失**（前期 `ls` 尚能看到活跃日志文件，随后 `tail` 提示目录不存在）。当前容器内已无该目录。 |
| 模型下载 | ⚠️ 需关注 | `/models/.download-progress/` 存在 12 条下载进度记录。多数任务 `speed=0` 且久无变化（`hf-internal-testing/tiny-random-gpt2`、`tiny-random-bert`、`Qwen1.5-0.5B` 等），疑为**下载卡死或残留记录**。另有 2 条 `Qwen2.5-0.5B-Instruct` 记录显示 speed>0，但容器内**无外网 443 下载连接**，可能已完成但进度文件未清理。 |
| 模型目录空间 | ✅ 正常 | `/models/` 下各目录占用：diffusion 321G、llama.cpp 40G、vllm 9.2G、lmstudio-community 62G 等，存量模型充足。 |

## 4. 自动修复动作
- **本次无可安全自动修复的异常**，原因如下：
  1. **日志目录消失**：无法判断是 AMM 主动清理/轮转、容器层问题，还是服务异常导致；贸然重建目录可能破坏 AMM 日志管理逻辑。
  2. **模型下载卡住**：当前 server.py 进程运行正常且提供推理服务，直接重启进程/容器以“清理”下载任务会中断线上服务，风险不可控。
  3. **无 `.git/index.lock` 或配置错误**：无需修复。

> **结论**：上述异常均需人工判断，未执行破坏性/风险操作。

## 5. 需老板介入点
1. **日志目录去向**：请确认 `/amm/deploy/logs/` 被清空/删除是否符合预期（如日志归档策略变更）。若不符合，请检查容器 volume 挂载及 AMM 后端日志写入逻辑。
2. **模型下载队列清理**：`/models/.download-progress/` 中存在大量停滞的旧进度记录，且部分记录大小异常（如 `Qwen1.5-0.5B` 显示 301,823 MB）。建议通过 AMM WebUI 或直接检查 server.py 日志（若可恢复）确认下载队列状态，并清理无效/卡住的进度文件。
3. **容器时钟偏差**：容器内日志时间戳出现 `Aug 9 00:51`（晚于巡检触发时间 00:48），建议校正容器 NTP/时区，避免后续日志时间线混乱。

## 6. 下一步计划
- 继续按 15 分钟周期进行巡逻，观察日志目录是否自动重建。
- 若日志目录持续缺失，建议在下次巡逻时尝试重启 AMM 容器（`docker restart AMM`）观察是否恢复，但**需先取得老板确认**。
- 密切监控模型下载进度文件变化，若新增记录持续 `speed=0` 超过 1 小时，可判定为下载服务异常。

---
*报告已随本轮巡逻自动提交并推送至 origin master。*
