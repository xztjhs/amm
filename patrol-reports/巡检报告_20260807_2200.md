# AMM 巡检报告 2026-08-07 22:00

**1) Git 状态**
- `git status` 干净，无变更需提交/推送
- 远端 `iei/amm` 已是最新 (Everything up-to-date)
- 最新提交: `e77cfcb` fix(perf): 后端去阻塞化，修复 Chat 模型选择/引擎切换/启停异常

**2) 服务健康**
- ⚠️ 当前无 vLLM/Diffusers 进程运行，服务自 **8/5 16:33** 停止
- 日志正常（最后记录为正常停止），无 crash/报错残留
- `.git/index.lock` 不存在，无坏状态

**3) 模型/验证状态**
- Wan2.2-T2V-A14B/I2V-A14B：已在 8/7 完成端到端验证（T2V + I2V 均通过），产物在 `verification/`
- 模型目录 `/models/zoo/modelscope` 尚不存在（当前主机无 GPU，推理服务无法启动）

**4) 自动修复**
- 无，无异常可安全自动修复

**⚠️ 仍需老板介入**
- **GPU 节点部署**：当前 `claw` 纯 CPU，无法承载 AMM 推理服务
- `deploy/docker-compose.yml` + `docker-entrypoint.sh` 已就绪（含 sshd + AMM 前台启动），待 GPU 环境 `docker compose up -d`

**下一步计划**
- 继续按 v0.4 稳定推进，代码/配置/脚本已就绪
- 等 GPU 环境就绪后执行容器化部署启动，再进行端到端联调
