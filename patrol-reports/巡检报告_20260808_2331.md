# AMM 项目巡检报告

- **巡检时间**：2026-08-08 23:31 (Asia/Shanghai)
- **巡检人**：cron-auto (amm-project-progress)
- **版本 / 分支**：master / `3e37ae7`

## 1. Git 推送情况

| 项目 | 状态 |
|------|------|
| 是否有未提交变更 | ❌ 无（工作区完全 clean） |
| 是否有未推送 commit | ❌ 无（已同步 origin/master） |
| `git push origin master` | ✅ Everything up-to-date |
| 最近一次提交 | `3e37ae7` auto: wip progress 2026-08-08 23:16:00 |
| .git/index.lock | ✅ 不存在 |
| merge/conflict 状态 | ✅ 正常，无冲突 |

## 2. 近 15 分钟变更（23:16 → 23:31）

- **代码/配置文件**：无新增或修改文件。
- **巡检报告**：本报告为新增文件，将随本轮一起提交。

## 3. 健康状态

### 3.1 Git 仓库
- 工作区干净，无需提交。
- 无 `.git/index.lock` 残留，无 merge 冲突。
- 最近几条提交均为 "auto: wip progress"，自动提交链路正常。

### 3.2 引擎 / 服务进程
- **vllm / diffusers / 推理进程**：当前无活跃进程（grep 未命中）。
- **AMM 容器**：`docker ps` 未发现 AMM 相关容器，服务当前未运行。
- **日志时戳**：`deploy/logs/` 下各日志最后更新为 **2026-08-05 17:07**，说明服务自 8/5 后已停止。

### 3.3 日志异常扫描（tail）
- `amm_server.log`：最后记录显示 8/5 16:32 AMM 正常启动（http://0.0.0.0:8080），加载 9 个模型配置，随后注册/启动 vllm chat 引擎（PID=26836），正常响应 `/health` 和 /v1/chat/completions。
- `chat_server.log`：vllm 引擎正常启动并进入 idle 状态（KV cache 0.0%，Running/Waiting: 0 reqs），**无报错或异常退出信息**。
- 结论：上次停机为正常停止（或等待重启的开发间歇），日志无异常报错。

### 3.4 模型 / 下载目录
- `models/`：在当前仓库根目录下不存在。
- `downloads/`：不存在。
- 说明：模型文件存放于宿主机独立挂载路径 `/models/...`，不在 git 管理范围，属预期行为。

## 4. 自动修复动作

- 本次巡检未发现需要自动修复的异常（无 git 坏状态、无 index.lock、无引擎崩溃、无配置错误）。
- AMM 服务未运行属于**开发间歇期正常停机**，无需自动重启（若需重启请老板确认）。

## 5. 需人工介入点

> **当前无需人工介入。**

若后续需要恢复在线验证，可手动执行：
```bash
cd /data/xtao/amm && docker restart AMM
# 或按 AMM 项目的自定义启动流程恢复
```

## 6. 下一步计划

1. 继续按 v0.6 路线图推进后端重构 / 多引擎调度等开发任务。
2. 当代码有实质变更时，自动提交链路会继续生效。
3. 后续巡检若发现引擎崩溃、git 冲突、`index.lock` 残留等问题，将尝试自动修复并上报。

---
*报告生成完毕，已随 git 提交并推送到内网 Gitea(iei/amm)。*
