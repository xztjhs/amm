# AMM 巡检报告 2026-08-07 22:15

**1) Git 状态**
- `git status` 干净，无新变更需提交/推送
- 远端 `iei/amm` 已是最新 (Everything up-to-date)
- 最新提交: `8b10b43` fix(vllm): _resolve_python 遍历已安装版本目录修复 engine_version 空时回退问题（22:00后新增）

**2) 服务健康**
- ⚠️ 当前无任何 AMM/vLLM/llama.cpp/Diffusers 进程运行，服务仍处于停止状态（自 8/5 16:33 起）
- Docker 当前不可用（`docker ps` 返回空），无法通过 docker compose 启动
- 日志无新增（最后为 8/5），无 crash/报错残留
- `.git/index.lock` 不存在，无坏状态

**3) 模型/验证状态**
- `/models` 目录不存在，模型尚未下载到当前主机
- Wan2.2-T2V-A14B 冷启动验证状态：待定（需 GPU 环境 + 模型下载后方可验证）
- 历史验证产物在 `verification/`（此前在 GPU 节点完成过端到端验证）

**4) 近 15 分钟变化**
- 22:00 → 22:15 期间代码无变更，只有历史自动 wip progress 提交
- 新增提交 `8b10b43`（vllm Python 路径修复），整体质量稳定

**5) 自动修复**
- 无。当前异常（无 GPU、无 Docker、无模型）属于环境/基础设施层面，无法安全自动修复。

**⚠️ 需老板介入**
- **GPU 节点部署**：当前 `claw` 为纯 CPU 环境，推理服务无法启动，需切到 GPU 节点运行
- **Docker 环境**：目标 GPU 节点需预装 Docker + nvidia-container-toolkit
- **模型下载**：GPU 节点就绪后需拉取 `/models`（Qwen3-4B、Wan2.2-T2V-A14B 等，约数十 GB）

**下一步计划**
- 代码/配置/脚本已就绪（docker-compose + entrypoint 已更新），v0.1 稳定推进中
- 待 GPU 环境就绪后执行 `docker compose up -d` 启动容器，再进行端到端联调和 Wan2.2 冷启动验证
