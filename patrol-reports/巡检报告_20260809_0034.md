# AMM 巡检报告 — 2026-08-09 00:34

## Git 推送情况
- **推送成功**：`master` → `http://192.168.100.245:60005/iei/amm.git`
- **最新提交**：`bbb4e97` auto: wip progress 2026-08-09 00:34:11
- **仓库状态**：干净，无未提交冲突，无 `.git/index.lock` 残留。

## 近15分钟变更
- `bbb4e97` — 自动提交：新增 docs/manual/ 产品手册 7 篇 + README，补充截图 6 张；前后端小幅更新（backend/server.py、frontend/css/style.css、frontend/index.html、frontend/js/app.js）。18 文件，+1596/-3 行。
- `044d22a` — 上一轮巡检报告更新（00:21）。
- `7b513b5` — docs(v0.8.0)：容器瘦身与部署配置仓库化维护记录。

## 健康状态
| 检查项 | 状态 | 说明 |
|---|---|---|
| Git 仓库 | ✅ 正常 | 无冲突、无锁文件、推送成功 |
| 推理引擎 (vllm/diffusers) | ⚠️ 未运行 | 宿主机未检测到 vllm / diffusers / python api 进程 |
| 服务日志 | ⚠️ 静止 | 最后日志为 08-05 16:33，各子服务（asr/embedding/ocr/tts）末尾显示正常退出 "cleaning up before exit..."，非崩溃 |
| Docker 容器 | ⚠️ 未运行 | 宿主机 `docker ps` 未列出 AMM 容器 |
| 模型下载 | ✅ 无活跃任务 | install 日志最后更新 08-05 17:07，无进行中的下载 |

## 自动修复动作
- 无。当前状态为服务未启动，属预期休眠，非故障，未执行高危自动重启。

## 需老板介入
1. **确认是否启动服务**：当前引擎与容器均未运行，如需恢复开发/测试环境，请确认从宿主机还是 AMM 容器内操作（部署目录 `/mnt/data/docker/app/amm/` 或 `docker restart AMM`）。
2. **手册与截图已入库**：docs/manual/ 与 screenshots 已推送到内网 Gitea，如需同步到对外 GitHub 仓库，需记得先脱敏再推 `github` remote。

## 下一步计划
- 保持每 15 分钟巡检节奏，继续提交增量进度。
- 待老板确认后如需启动服务，将执行 `docker restart AMM` 或容器内启动 server。
- v0.6 核心代码持续推进中，如需合并分支或打 tag 请告知。
