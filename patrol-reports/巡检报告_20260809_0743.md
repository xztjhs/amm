# AMM 项目巡检报告 — 2026-08-09 07:43

> 巡检周期：15分钟 | 仓库：`/data/xtao/amm` | 分支：`master` | 远程：`origin → http://192.168.100.245:60005/iei/amm.git`

---

## 1. Git 推送情况

- **工作区状态**：干净，无未跟踪/未提交文件
- **最近一次提交**：`3ce57c8` — `auto: 巡检报告 2026-08-09 07:31:25`
- **近15分钟变更**：仅 `patrol-reports/巡检报告_20260809_0728.md`（上一轮报告）
- **推送状态**：`master` 已同步至内网 Gitea（`iei/amm`）
- **异常**：无 merge 冲突，无 `.git/index.lock` 残留

---

## 2. 近15分钟变更

- 无代码变更
- 无配置变更
- 仅生成巡检报告文件

---

## 3. 健康状态

### 3.1 服务进程

| 服务 | 状态 | 备注 |
|------|------|------|
| AMM 主服务（server.py） | **未运行** | 自 8月5日 后未检测到进程 |
| vLLM 引擎 | 未运行 | — |
| llama.cpp 各模型服务 | 未运行 | chat/embedding/asr/tts/reranker/ocr 均未启动 |
| diffusers（t2i/t2v/i2v） | 未运行 | — |
| 系统 tuned | 正常 | 系统服务 |

### 3.2 日志健康（`deploy/logs/`）

- 所有日志最后更新时间：**2026-08-05**，近期无新增日志
- 历史异常记录（8月5日）：
  - `chat_server.log`：多次 `RuntimeError: Engine core initialization failed`（GPU 显存不足：15.5/83.05 GiB < 目标 53.98 GiB）
  - `chat_server.log`：多次 `Error with model error='The model chat does not exist.'`
  - `ocr_server.log`：`exiting due to HTTP server error`
  - `embedding_server.log`：`n_batch = n_ubatch = 512` 警告
- 结论：日志**无新增报错**，上述均为历史记录

### 3.3 模型与下载

- `/models` 目录（宿主机映射）：非仓库内路径，未在本地检查
- 仓库内无 `models/`、`engines_installed/` 目录
- 无 `.tmp` / `.part` / `.downloading` 等下载中文件
- 无可进行中的模型下载任务

### 3.4 磁盘与资源

- 磁盘使用率：`5%`（498G 总量，22G 已用，477G 可用）✅
- 无 `.git/index.lock` 残留 ✅
- 无未推送分支/提交 ✅

---

## 4. 自动修复动作

本轮无可安全自动修复的异常：

- ✅ `.git/index.lock` 不存在，无需清理
- ✅ 无 merge 冲突
- ✅ 无崩溃引擎进程残留（需清理的 PID 不存在）
- ⏸️ **AMM 全套服务未运行**：涉及 GPU 资源配置、容器启动策略、模型文件是否存在等，**需老板判断是否手动启动**
- ⏸️ **模型文件是否就绪**：配置指向 `/models`（宿主机/容器映射），仓库内无法确认，**需人工核实**

---

## 5. 需老板介入点

1. **AMM 服务启动决策**：自 8月5日 后全套推理服务未运行，是否启动？
   - 前期失败原因包括：GPU 显存不足、模型 `chat` 未找到
   - 启动前建议确认 `/models` 下模型文件是否齐全
2. **GPU 资源配置**：若 vLLM 仍报显存不足，可能需要调整 `gpu_memory_utilization` 或释放其他 GPU 进程

---

## 6. 下一步计划

- [ ] 等待老板确认是否启动 AMM 服务及启动条件
- [ ] 若启动，优先验证 `/models` 目录下模型文件完整性
- [ ] 若采用 llama.cpp 替代 vLLM（当前配置默认 engine 已改为 llama_cpp），测试单服务启动
- [ ] 持续 15 分钟巡检，监控服务恢复后的日志健康

---

*巡检时间：2026-08-09 07:43 (Asia/Shanghai)*
*巡检人：SkillSmith（自动巡检）*
