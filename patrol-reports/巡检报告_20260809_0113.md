# AMM 项目巡检报告

| 项目 | 内容 |
|------|------|
| 巡检时间 | 2026-08-09 01:13 CST |
| 巡检周期 | 15分钟 |
| 仓库路径 | /data/xtao/amm |
| 当前分支 | master |
| 最新提交 | 24a1f50 auto: 巡检报告 2026-08-09 01:03:20 |

---

## 1. Git 推送情况

- **本地分支**: master
- **远程跟踪**: origin/master (内网 Gitea: iei/amm)
- **同步状态**: ✅ 已同步，无未推送提交
- **未提交变更**: 无
- **冲突状态**: 无

## 2. 近15分钟变更（01:03 → 01:13）

- 上一次提交时间：01:03:20
- 本周期内无新的代码变更
- 自 01:03 以来 workspace 无变动

## 3. 健康状态检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Git 工作区 | ✅ 正常 | 干净，无冲突，无 index.lock |
| 磁盘空间 | ✅ 正常 | /dev/vda2 477G 可用，使用率 5% |
| AMM 容器 | ⚠️ 未运行 | amm-server 容器不存在（docker ps -a 无结果） |
| 推理日志 | ⚠️ 陈旧 | deploy/logs/ 最后更新 2026-08-05 |
| 模型目录 | ⚠️ 未挂载 | /data/xtao/amm/models/ 不存在（宿主机通过 volume 挂载） |

### 容器状态详情

- `docker ps -a` 中无 `amm-server` 容器
- 日志目录 `deploy/logs/` 下文件时间戳为 2026-08-05，表明容器已停止约 4 天
- 最后一次日志记录显示 AMM 正常启动并注册了 vllm/llama.cpp/diffusers 引擎，模型 chat (Qwen3-4B) 已启动

## 4. 自动修复动作

- ✅ Git 工作区正常，无需修复
- ✅ 无 index.lock 残留
- ⚠️ AMM 容器缺失 — **未自动重启**，原因如下：
  - 容器已停止约 4 天，可能是有意停机维护或资源释放
  - 重启需执行 `docker compose -f deploy/docker-compose.yml up -d`，涉及 nvidia runtime 及 GPU 资源分配
  - 模型文件挂载路径 `/models` 需确认宿主机上存在（当前 `/data/xtao/amm/models/` 不存在，但 compose 中挂载的是 `/data/models`）
  - **建议老板确认是否需要启动容器**

## 5. 下一步计划

1. **待确认**：是否启动 AMM Docker 容器（`deploy/docker-compose.yml`）
2. **待确认**：模型文件 `/data/models` 是否在宿主机上就绪
3. 若确认启动，执行：
   ```bash
   cd /data/xtao/amm/deploy && docker compose up -d
   ```
4. 持续按 15 分钟周期巡检 Git 状态及容器健康

---

> 本报告由自动巡检生成，如有异常需人工介入已标注 ⚠️
