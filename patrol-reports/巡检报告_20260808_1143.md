# AMM 项目 11:43 巡检报告
> 时间: 2026-08-08 11:43 CST (周六) | 巡检周期: 15分钟

---

## 1) Git 进度推送 ✅
- **最新提交**: `80f8995` — fix(vllm): FlashInfer JIT 缺 ninja 报错；已在 start_process 注入 venv bin 到 PATH + /usr/local/bin/ninja 软链；vLLM 自动选 FLASH_ATTN 后端；实测 Qwen3-4B 完整启动成功
- **近15分钟变更**: ⭐ **有实质提交**（11:15→11:43）。上轮回检为 `790c438`，本轮发现新提交 `80f8995`，系 vLLM 引擎启动修复。
- **推送状态**: `master` → `origin/master`，iei/amm 已同步 ✅
- **Git 健康**: ✅ 干净，无 merge 冲突、无 `.git/index.lock` 残留

## 2) 健康状态

| 检查项 | 状态 | 说明 |
|---|---|---|
| Git 仓库 | ✅ 正常 | clean，无冲突、无 lock |
| AMM 主服务 | ➖ 未运行 | 当前节点无 GPU，非目标运行环境，符合预期 |
| vllm/diffusers 进程 | ➖ 未运行 | 同上，符合预期 |
| 日志异常 | ✅ 无新增 | deploy/logs/ 仍为 8/5 历史日志，无新 ERROR/FATAL |
| 系统负载 | ✅ 1.58 / 1.55 / 1.64 | 正常 |
| 磁盘空间 | ✅ /dev/vda2 21G/498G（5%） | 正常 |
| 内存 | ✅ 15G 总 / 7.7G 已用 / 7.6G 可用 | 正常无压力 |

## 3) 模型与验证状态
- **Wan2.2-T2V-A14B**: 8/7 冷启动验证通过 ✅（131KB 输出视频已落盘）
- **Wan2.2-I2V-A14B**: 8/7 验证通过 ✅（23KB 输出视频已落盘）
- `/models/` 目录：当前节点不存在（目标部署节点需挂载 `/models/zoo/modelscope`）

## 4) 自动修复
无需执行。近15分钟无新增异常，无崩溃进程、无 lock 残留、无配置错误可安全自动修复。

## 5) 下一步计划
仍按 **v0.4 稳定就绪** 推进：
- 当前节点为纯代码/巡检节点
- 等待 GPU 目标节点就绪后执行容器化部署与端到端联调

> ⚠️ **需老板关注**: GPU 目标部署节点及 `/models/` 挂载方案确认，以便继续推进 v0.5 端到端验证。

---
> 报告由 cron 巡检自动生成。
