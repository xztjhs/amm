# AMM 项目 15 分钟例行巡检报告

| 字段 | 内容 |
|------|------|
| 巡检时间 | 2026-08-09 02:43:00 (Asia/Shanghai) |
| 巡检轮次 | 自 02:30 后的第 2 轮 |
| 报告人 | cron auto-patrol |

---

## 1. Git 推送情况

- **本地提交状态**：工作区干净，无未提交/未推送变更
- **远程同步**：`git push origin master` → `Everything up-to-date`
- **目标仓库**：内网 Gitea `iei/amm` (http://192.168.100.245:60005)
- **最新提交**：`6739d8c auto: 巡检报告 2026-08-09 02:30`
- **无冲突/无 index.lock**：正常

## 2. 近 15 分钟变更

- 无代码变更。
- 最近一次提交为 02:30 自动生成的上一期巡检报告。

## 3. 健康状态

### 3.1 Git / 仓库健康
- ✅ 工作区干净
- ✅ 无 merge 冲突
- ✅ 无 `.git/index.lock` 残留
- ✅ 远程推送正常

### 3.2 服务进程健康
- ⚠️ **AMM Server**：**未运行**（无进程，端口 8080 未监听）
- ⚠️ **Chat (vllm)**：**未运行**（无进程，端口 18081 未监听）
- ⚠️ **Embedding (llama_cpp)**：**未运行**
- ⚠️ **ASR (llama_cpp)**：**未运行**
- ⚠️ **TTS (llama_cpp)**：**未运行**
- ⚠️ **OCR (llama_cpp)**：**未运行**
- ⚠️ **Docker 容器**：无 amm 相关容器在运行

> 日志最后活跃时间：所有服务日志在 **2026-08-05 16:33** 左右停止，至今已约 **3 天 10 小时**。

### 3.3 模型与存储
- ⚠️ `/models` 目录**缺失**，但 `models_config.yaml` 中 `models_dir: "/models"`。
- 磁盘空间充裕：`/` 使用率仅 5%（477G 可用）。

### 3.4 Diffusers 组件
- 未发现 diffusers 进程运行。
- `deploy/diffusers_ctl.sh` 脚本存在，当前处于可调用状态。

## 4. 自动修复动作

- 无需清理 index.lock（不存在）。
- 未执行自动重启：服务长时间离线涉及容器编排、模型目录挂载及外部配置（端口映射、GPU 可用性、模型文件完整性），属于**需人工判断的介入点**，未擅自重启。

## 5. 需老板介入点

1. **AMM 全服务已离线 3 天+**：需确认是否预期内停机，或需重启 Docker / systemd 服务。
2. **`/models` 目录缺失**：需确认模型文件存储位置（是否在其他挂载点或需重新下载）。
3. **重启前检查清单**：
   - Docker 服务状态 & `deploy/docker-compose.yml` 是否需要更新？
   - GPU 驱动 / CUDA 环境是否正常？
   - `/models` 对应宿主机路径是否正确映射？

## 6. 下一步计划

- **若老板确认重启**：执行 `docker-compose -f deploy/docker-compose.yml up -d` 并在拉起后验证各引擎健康接口。
- **若需恢复模型**：可跑 `deploy/scripts/dl_all.sh` / `dl_one.py` 补全模型（需先确认目录挂载）。
- **稳定后**：按 v0.6 节奏继续推进 diffusers 高级参数与显存优化、引擎注册抽象等后续迭代。

---

*报告生成时间：2026-08-09 02:43 (Asia/Shanghai)*
