# AMM 项目巡检报告 — 2026-08-09 06:13

## 1. Git 推送情况
- **本地 HEAD**: `055592c` — `auto: wip progress 2026-08-09 06:04:52`
- **远程 HEAD**: `055592c` (与本地同步)
- **分支状态**: `master` 已推送至 `origin/master` (iei/amm @ 192.168.100.245:60005)，工作区干净
- **近 15 分钟变更**: 无代码变更（上条 commit 为 06:04 自动巡检提交）
- **推送结果**: ✅ 无需提交，远程已是最新

## 2. 健康状态
- **Git 状态**: ✅ 正常，无冲突、无 index.lock、无未推送提交、无 merge 状态
- **本地进程**: ⚠️ 无 AMM / vLLM / diffusers / Python 服务进程在运行（符合预期，服务部署于远程容器）
- **远程连接**: ❌ 仍然异常 — `192.168.100.245:62022` TCP 端口可通，但 SSH 握手持续超时，无法登录容器宿主机
- **服务日志**: 容器外日志目录 `logs/` 为空；`deploy/logs/` 最后更新为 **2026-08-05 17:07**，已停滞约 **4 天**
- **服务状态**: 无法确认（远程不可达），日志停滞推断服务可能已离线

## 3. 自动修复动作
- ✅ 清理本地僵死 SSH 进程 **1** 个（巡检任务残留）
- ✅ 本报告已生成并将随 git 推送
- ⚠️ 本地无可执行代码变更，未产生新的 `wip progress` commit
- ⚠️ 远程 SSH 仍不可达，无法执行容器内修复

## 4. 开发进展异常
| 检查项 | 结果 | 说明 |
|--------|------|------|
| Git 冲突 / 坏状态 | ✅ 正常 | 无冲突、无 index.lock、无 merge 状态 |
| 未推送提交 | ✅ 正常 | 本地与远程 HEAD 一致 |
| 引擎进程异常 | ⚠️ 不可知 | 远程不可达，无法检查容器内进程 |
| 模型下载状态 | ✅ 正常 | 本地无 `.tmp/.part/.downloading` 残留文件，无进行中的下载 |
| deploy/logs 更新 | ⚠️ 异常 | 自 08-05 17:07 后无更新，停滞 4 天 |

## 5. 需老板介入点
> **🔴 优先级：高**（与上轮一致，仍未恢复）
>
> 1. **远程服务器 SSH 仍不可用**：`192.168.100.245:62022` TCP 端口通但 SSH 握手超时，无法连接容器宿主机确认 AMM 服务状态。
> 2. **服务离线约 4 天**：`deploy/logs/` 及容器日志自 08-05 17:07 后无更新。
> 3. **建议操作**：
>    - 检查宿主机 SSH 服务是否正常运行（`systemctl status sshd` 或重启 sshd）
>    - 确认 `amm_contain_id_rsa` 公钥是否仍被宿主机授权
>    - 服务器恢复后检查 Docker 容器 `amm-server` 状态：`docker ps | grep amm`
>    - 若容器未运行，执行 `docker compose up -d` 或 `bash deploy/amm.sh start`
>    - 检查 `/data/models` 挂载与模型文件完整性

## 6. 下一步计划
- [ ] 等待老板恢复远程 SSH 连通性
- [ ] 恢复后执行：检查容器状态 → 检查日志 → 检查模型 → 按需重启服务
- [ ] 当前代码版本 **v0.8.0**（用户手册上线 / 容器瘦身 / 部署配置仓库化），无本地未推送改动

---
*巡检时间*: 2026-08-09 06:13 (Asia/Shanghai)
*巡检人*: cron — auto patrol
