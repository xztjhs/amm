# AMM 项目巡检报告

- **巡检时间**: 2026-08-08 17:55 (Asia/Shanghai)
- **巡检项**: Git 状态 / 进程健康 / 模型下载 / 服务运行状态
- **执行人**: amm-project-progress cron job

---

## 1. Git 推送情况

| 项目 | 状态 |
|------|------|
| 当前分支 | `master` |
| 最新提交 | `86150b0 auto: patrol report 2026-08-08 17:37:00` |
| 未推送提交 | 无 (`origin/master..master` 为空) |
| 工作目录变更 | 无 (`git status --short` 为空) |
| `.git/index.lock` | 不存在 |
| merge 冲突 | 无 |

**结论**: Git 仓库状态健康。本次巡检报告写入后将随 `git add -A` 一并提交并推送至内网 Gitea (`iei/amm`)。

---

## 2. 近 15 分钟变更 (17:40 → 17:55)

- **代码变更**: 无
- **提交记录**: 无新 commit（距上次提交 17:37 约 18 分钟）
- **文件变化**: 仅新增本巡检报告

---

## 3. 健康状态检查

### 3a) Git 仓库健康
- ✅ 无未提交冲突
- ✅ 无 `.git/index.lock` 残留
- ✅ 无未推送 commit

### 3b) 引擎 / 服务进程
- ❌ **AMM Docker 容器未运行** (`docker ps` 无 amm-server)
- ❌ **无 vLLM 进程** (最近一次运行为 2026-08-05 16:33)
- ❌ **无 diffusers 进程**
- ⚠️ 仅有系统 tuned 守护进程在运行

> 注：AMM 服务最后一次运行记录为 2026-08-05，目前无活跃推理进程。若为预期停机（如维护、调试），此状态正常；若为意外退出，需人工介入排查。

### 3c) 模型文件检查
- ❌ **`/data/models/` 为空目录**
- ❌ **`/data/xtao/6kd-ai-models-manage/models/` 为空目录**
- ⚠️ 配置 `models_config.yaml` 指向 `/models`（容器内映射 `/data/models`），但宿主机目录未存放任何模型权重

> 从 CHANGELOG 记录看，T2V 模型 (Wan2.2-T2V-A14B) 上次记录的下载进度为 58G/76G (2026-08-05)，当前模型目录为空，下载可能已中断或未完成，需确认。

---

## 4. 自动修复动作

| 发现的异常 | 是否尝试自动修复 | 结果 / 原因 |
|-----------|----------------|------------|
| `.git/index.lock` 残留 | 否 | 不存在，无需修复 |
| 未推送 commit | 否 | 不存在，无需修复 |
| merge 冲突 | 否 | 不存在，无需修复 |
| AMM 服务未运行 | **否** | 涉及 Docker 容器管理，需判断是维护停机还是异常崩溃，属于需人工判断的场景 |
| `/data/models` 为空 | **否** | 模型下载涉及外部凭据（ModelScope/HuggingFace）、大文件存储和带宽，不适合自动续传 |

---

## 5. 需老板介入点

1. **AMM 服务运行状态**: Docker 容器未运行，无推理进程。请确认是否为计划内停机，或需要重新启动服务。
2. **模型下载进度**: `/data/models` 目录为空。CHANGELOG 记录 T2V 模型下载中 (58G/76G)，请确认下载是否已完成、迁移至其他路径，或需要重新发起下载。
3. **v0.6 版本推进策略**: 目前项目代码保持干净，但运行环境（模型 + 服务）未就绪，需确认下一步是重点补全模型资产，还是优先推进代码功能。

---

## 6. 下一步计划

- [ ] 确认 AMM 服务是否需要重新启动（docker compose up -d）
- [ ] 确认模型文件存储路径及下载进度
- [ ] 恢复 `/data/models` 下的模型资产后继续 v0.6 功能验证
- [ ] 若服务重启，验证 vLLM / diffusers / llama.cpp 各引擎启动及 API 可用性

---

*报告生成: 2026-08-08 17:55*  
*仓库: `/data/xtao/amm` → `http://192.168.100.245:60005/iei/amm.git`*
