# AMM 项目巡检报告

**巡检时间：** 2026-08-08 14:58 (Asia/Shanghai)  
**巡检人：** cron / skill-agent  
**版本：** 基于 commit `fe2df05`

---

## 1. Git 推送情况

- **本地状态：** 干净，无未提交/未推送变更
- **最近一次提交：** `fe2df05 auto: patrol report 2026-08-08 14:46:41`（已推送至 origin/master）
- **远程：** `origin http://192.168.100.245:60005/iei/amm.git` 可达，HTTP 200
- **近 15 分钟变更：** 无新代码变更（上轮提交后工作区无改动）
- **异常检查：** 无 `.git/index.lock`、无 merge 冲突、无坏状态

---

## 2. 服务与健康状态

| 服务 | 状态 | 说明 |
|------|------|------|
| amm_server (主控) | ⏹️ 停止 | 最后活动 2026-08-05 16:32，日志正常退出 |
| chat (vllm/llama_cpp) | ⏹️ 停止 | 最后 PID=26836，日志正常退出 |
| asr (llama.cpp) | ⏹️ 停止 | 日志显示正常 cleanup exit |
| tts (llama.cpp) | ⏹️ 停止 | 同上 |
| ocr (llama.cpp) | ⏹️ 停止 | 同上 |
| embedding (llama.cpp) | ⏹️ 停止 | 同上 |

**健康评估：** 所有推理服务自 8 月 5 日停止运行，但日志均显示正常退出（`cleaning up before exit...`），无崩溃、无异常报错。推测可能因维护/安装流程暂停。

---

## 3. 正在进行的操作

- **远程依赖安装/验证：** PID 177822/177869 正在通过 sshpass → 192.168.100.245:62022 进行 Python 依赖安装与验证
  - 已确认：`torch-2.11.0+cu130`、`triton-3.6.0`、`cuda-toolkit-13.0.2` 等安装成功
  - 当前步骤：验证 `diffusers`、`numpy`、`torch`、`transformers`、`einops` 导入
- **说明：** 此为部署/安装流程的一部分，进程运行正常，未卡住（约 2 分钟前启动）

---

## 4. 模型下载状态

- **配置路径：** `/models`（来自 `models_config.yaml`）
- **实际状态：** `/models` 目录在当前宿主机不存在
- **说明：** 模型文件可能位于远程部署节点（192.168.100.245），或尚未下载。需确认部署架构（当前安装流程正在远程节点执行）。

---

## 5. 自动修复动作

本轮无可安全自动修复的异常：
- ✅ 无 `.git/index.lock` 残留 → 无需清理
- ✅ 无崩溃进程 → 无需重启引擎
- ✅ 无配置错误 → 无需修复
- ⏳ 服务停止状态与正在进行的远程安装流程强相关，**不自动重启服务**，避免与安装流程冲突

---

## 6. 需人工介入点

1. **确认服务重启时机：** 所有推理服务已停止约 3 天，当前远程依赖安装/验证进行中。请在确认远程节点（192.168.100.245）依赖安装全部完成后，手动触发服务启动（`deploy/amm.sh` 或 `docker-compose up`）。
2. **确认模型存放位置：** `/models` 目录在本地不存在，请确认模型是否已下载至远程节点或需要重新拉取。
3. **确认 v0.6 版本进度：** 当前代码无新提交，请确认是否仍在 v0.6 功能冻结/测试阶段。

---

## 7. 下一步计划

1. 继续监控远程依赖安装/验证进程（预计 15 分钟后下一巡检轮次再次确认）
2. 待远程节点准备完成后，验证模型就绪并启动 AMM 全套服务
3. 继续进行例行 git 提交与巡检报告归档

---

*报告由自动巡检生成，如有异常请优先查看 "需人工介入点" 部分。*
