# AMM 项目巡检报告

**巡检时间**: 2026-08-08 17:07 (Asia/Shanghai)  
**巡检周期**: 15分钟  
**项目路径**: /data/xtao/amm  
**当前版本**: v0.6.4（模型下载增强：代理 + 版本/文件选择 + 断点续传/进度）

---

## 1. Git 推送情况

- **最新提交**: `92fe584` auto: patrol report 2026-08-08 16:54:33
- **推送状态**: ✅ 已推送至 origin/master（内网 Gitea iei/amm）
- **分支状态**: master 与 origin/master 同步，无未推送提交
- **仓库状态**: 干净（无未提交更改、无冲突、无 .git/index.lock）

## 2. 近15分钟变更（16:52 – 17:07）

- 代码文件：无新增或修改
- 配置文件：无变更
- 日志文件：无更新（日志最后写入时间为 8月5日 17:07）

## 3. 健康状态检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Git 仓库健康 | ✅ 正常 | 无冲突、无 lock 文件、无未合并分支 |
| AMM 主服务进程 | ⚠️ 未运行 | 无 python3.11 backend/server.py 进程 |
| vLLM 引擎进程 | ⚠️ 未运行 | 无 vllm 相关进程 |
| Diffusers 引擎进程 | ⚠️ 未运行 | 无 diffusers 相关进程 |
| 服务日志更新 | ⚠️ 停滞 | 所有日志最后更新于 8月5日 17:07，已停滞约 3 天 |
| 模型目录 /models | ⚠️ 不存在 | 当前环境非 AMM 模型/服务运行环境 |
| GPU 状态 | ⚠️ 无 GPU | 无 NVIDIA 显卡，nvidia-smi 不可用 |

## 4. 自动修复动作

- **Git 健康**: 无需修复，状态正常。
- **.git/index.lock**: 不存在，无需清理。
- **服务重启**: ⛔ **未执行**。当前机器（/data/xtao/amm）为代码仓库/开发环境，缺失 `/amm`、`/models` 目录及 GPU，不满足 AMM 服务运行条件。自动重启将导致失败或环境错误，故跳过。
- **模型下载续传**: ⛔ **未执行**。`/models` 不存在，当前环境非模型存储节点。

## 5. 异常与需老板介入点

1. **AMM 服务运行环境状态不明**
   - 现象：日志停滞于 8月5日 17:07，已超 3 天。
   - 判断：当前机器 `/data/xtao/amm` 为 Git 仓库与开发环境，AMM 服务脚本（`/amm/deploy/amm.sh`）指向 `/amm` 路径，而该路径在当前机器不存在。
   - 需确认：AMM 实际运行环境是否为另一台带 GPU 的服务器/容器？若服务已停止，应在目标环境上手动重启或排查停止原因。

2. **模型下载进度不可见**
   - 现象：`/models` 目录不存在，`backend/config/models_config.yaml` 配置模型目录为 `/models`，下载脚本目标路径亦在 `/models`。
   - 需确认：模型下载是在另一台机器/容器中进行，还是尚未开始？若在本地执行，需先创建 `/models` 并确保磁盘空间充足。

## 6. 下一步计划

1. **开发侧**：继续按 v0.6 稳定推进，v0.6.4 模型下载功能已开发完成并通过 webui 实测。
2. **部署侧**：待老板确认 AMM 服务运行环境后，在正确节点上执行服务巡检与重启。
3. **后续巡检**：如确认为纯开发仓库环境，后续可调整巡检策略，侧重代码提交健康度与开发进度跟踪。

---

*报告生成: 自动巡检脚本*  
*版本: v0.6.4*
