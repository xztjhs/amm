# 巡检报告 — AMM 项目

**巡检轮次**：2026-08-08 20:28:00 (Asia/Shanghai)  
**仓库路径**：`/data/xtao/amm`  
**版本**：`d85c312` (v0.7.3)  
**巡检周期**：15 分钟

---

## 1. Git 推送情况

| 项目 | 状态 |
|------|------|
| 本地提交 vs origin/master | ✅ 完全一致（HEAD = origin/master = d85c312） |
| 未推送提交 | 无 |
| 未提交变更 | 无（工作区干净，git status --short 为空） |
| 自动提交 | 本轮无新变更，无需自动 commit |
| 强制推送 | 无 |

**操作结果**：`git push origin master` → `Everything up-to-date`（内网 Gitea `iei/AMM` 已同步）

---

## 2. 近 15 分钟变更

| 时间 | 提交 | 说明 |
|------|------|------|
| 20:23 | `d85c312` | docs(v0.7.3): CHANGELOG + 维护记录（高级参数/自动释放/Playground 三能力） |
| 20:22 | `ebf787d` | fix(v0.7.3): yaml 用最小文本替换补 t2v/i2v label_en |
| 20:22 | `e68ffc3` | feat(v0.7.3): Advanced/自动释放/只读展示同步到 t2v/i2v Playground |
| 20:16 | `5a60325` | auto: wip progress（上轮自动提交） |

**进展**：v0.7.3 功能收尾阶段，主要完成 t2v/i2v Playground 的高级参数、自动释放、只读展示同步，以及 CHANGELOG 与维护记录更新。

---

## 3. 健康状态

### 3.1 Git 仓库
- `.git/index.lock`：❎ 不存在（无残留）
- Merge 冲突：无
- `git fsck`：仅检测到 1 个 dangling blob（正常现象）
- 远程配置：`origin` 指向内网 Gitea `iei/AMM` ✅

### 3.2 服务 / 引擎进程
- Docker 容器 `amm-server`：**未运行**
- vLLM / diffusers 推理进程：**未检测到**
- 系统 Python 进程：仅 `tuned`/`tuned-ppd`（系统服务，正常）

> ⚠️ AMM 服务当前离线。结合时间（周六 20:28），属预期内的非工作时段停机。

### 3.3 模型 / 存储
- 模型挂载点 `/data/models`：存在，磁盘使用率 5%（477G 可用）✅
- 专用模型下载目录：未在仓库内创建，模型由宿主 `/data/models` 挂载供给

### 3.4 日志
- 最新日志时间：`Aug 5 17:07`（容器的旧日志）
- 无新增报错日志（服务未运行）

---

## 4. 自动修复动作

本轮无需要自动修复的异常项：
- Git 状态健康 → 无需清理 index.lock 或解决冲突
- 无引擎崩溃 → 无需重启
- 无模型下载中断 → 无需续传

---

## 5. 需老板介入点

| 事项 | 建议 | 紧急程度 |
|------|------|----------|
| AMM 服务离线 | 如需周末联调 / 验证 v0.7.3，请指示是否启动 `amm-server` 容器（`cd /data/xtao/amm && docker compose up -d`） | 低 |

---

## 6. 下一步计划

- v0.7.3 文档与功能已同步完成，余量以稳定收尾为主
- 若老板确认，可安排容器启动 + 冒烟测试
- 持续按 15 分钟粒度巡检并自动提交 progress

---

*报告生成：OpenClaw Agent（amm-project-progress 定时任务）*
