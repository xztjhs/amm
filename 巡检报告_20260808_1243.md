# AMM 项目 12:43 巡检报告
> 时间: 2026-08-08 12:43 CST (周六) | 巡检周期: 15分钟

---

## 1) Git 进度推送 ✅
- **最新提交**: `de7952c` — docs: CHANGELOG v0.6 启动命令编排 + reasoning-budget (12:44)
- **上一提交**: `ee8b816` — feat(v0.6): 启动命令编排 - Models 卡片可基于参数生成/编辑/保存启动命令行, start 优先执行自定义脚本
- **推送状态**: ✅ 成功推送至内网 Gitea (iei/amm)，`Everything up-to-date`
- **Git 状态**: ✅ clean，无未提交变更、无 merge 冲突、无 `.git/index.lock` 残留

## 2) 近15分钟变化 (12:28→12:43)
- **CHANGELOG 文档更新**: v0.6 启动命令编排 + reasoning-budget 文档补全 (+16 行)
- **v0.6 功能已落地**: 启动命令编排 — Models 卡片支持基于参数生成/编辑/保存启动命令行，start 优先执行自定义脚本
  - 涉及文件: `backend/core/engine.py` (+2), `backend/core/model_manager.py` (+95), `backend/server.py` (+28), `frontend/js/app.js` (+89)
  - 合计: 227 行新增/修改

## 3) 健康状态

| 检查项 | 状态 | 说明 |
|---|---|---|
| Git 仓库 | ✅ 正常 | clean，无冲突、无 lock，推送成功 |
| AMM 主服务 | ➖ 未运行 | 当前节点无 GPU，非目标运行环境，符合预期 |
| vllm/diffusers 进程 | ➖ 未运行 | 同上，符合预期 |
| 日志异常 | ✅ 无新增 | deploy/logs/ 仍为 8/5 历史日志，无新 ERROR/FATAL |
| 系统负载 | ⚠️ 略高 | 2.68 / 2.12 / 1.87 (1min平均偏高，观察中) |
| 磁盘空间 | ✅ /dev/vda2 21G/498G（5%） | 正常 |
| 内存 | ✅ 15G 总 / 8.0G 已用 / 7.4G 可用 | 正常无压力 |

## 4) 模型与验证状态
- **Wan2.2-T2V-A14B**: 8/7 冷启动验证通过 ✅（verification/wan_t2v_480p_16steps_17frames_20260807-1121.mp4 131KB 已落盘）
- **Wan2.2-I2V-A14B**: 8/7 验证通过 ✅（verification/wan_i2v_480p_20steps_20260807-1842.mp4 23KB 已落盘）
- `/models/` 目录：当前节点不存在（目标部署节点需挂载 `/models/zoo/modelscope`）

## 5) 自动修复
无需执行。近15分钟无新增异常，无崩溃进程、无 lock 残留、无配置错误可安全自动修复。

## 6) 下一步计划
仍按 **v0.6 启动命令编排** 稳定推进：
- v0.6 功能代码已完成，CHANGELOG 已补齐
- 等待 GPU 目标节点就绪后执行容器化部署与端到端联调

## 7) 需老板关注
- **GPU 目标部署节点及 `/models/` 挂载方案确认**，以便继续推进 v0.5/v0.6 端到端验证。
- **1分钟负载 2.68 略高**：持续观察中，24天未重启，若持续走高可考虑低峰期重启释放累积负载。

## 8) 报告发送状态
- `sessions_send` 至 `agent:skill:main` 失败：`visibility=tree` 限制，当前会话树外不可见。
- 报告已落盘至本文件及 `.cron-progress-report.md`，请老板直接查看。

---
> 报告由 cron 巡检自动生成。
