# AMM 项目巡检报告 2026-08-07 13:43

## 1. 代码提交/推送
- **有变更，已提交并推送**: `ce440cc chore: 忽略 server.py 备份文件`
- 当前 HEAD: `ce440cc`，与 `origin/master` 同步（内网 Gitea: iei/amm）
- 无未推送提交、无冲突、无 `.git/index.lock`

## 2. 近15分钟变更 (ce440cc)
- **server.py**: 双端口(WebUI 80 + API 8080) → 统一单端口(80)，简化架构
- **models_config.yaml**: 同步更新端口配置，兼容保留旧字段
- **docker-compose.yml**: 移除 60008:8080 映射，只保留统一入口 60006:80
- **docker-entrypoint.sh**: 更新端口注释说明
- **清理**: 删除 `server.py.bak-202608071339` 备份文件
- **.gitignore**: 新增忽略规则避免备份文件再次入库

## 3. 健康检查
| 检查项 | 状态 | 备注 |
|---|---|---|
| Git 状态 | ✅ 正常 | working tree clean, up to date |
| 磁盘空间 | ✅ 正常 | / 分区使用 4%, 482G 可用 |
| 引擎进程 | ⚠️ 停止 | 无 vllm/diffusers/engine 进程运行 |
| 日志报错 | ✅ 无新增 | deploy/logs/ 最新为 8/5，无新错误 |
| 模型目录 | ⚠️ 未挂载 | /models 不存在，需 GPU 环境挂载 |
| GPU 环境 | ⚠️ 不可用 | nvidia-smi 未检测到，当前为 CPU 开发机 |

## 4. 自动修复
- 无异常，无需自动修复

## 5. 下一步
- 架构简化(v0.1.1)持续稳定，等待 GPU 环境部署验证或下一轮开发任务
- Wan2.2-T2V-A14B 冷启动在之前环境已验证通过，待 /models 挂载后可直接复用
